# Domain lock (GM EvolveX360, 2026-09-17)

`iceland365.com` is taken. It is not ours. Do not write it as an owned URL.

When renaming `IcelandNOW.com` / `icelandnow.com`:

1. Prefer brand text **Iceland365** (no TLD) where a domain is not required.
2. Prefer **Iceland365.is** when a domain string is needed.
3. If a `.com` string is required: **geticeland365.com** only.
4. Never write `iceland365.com` / `Iceland365.com` as if we own it.

This draft PR follows that lock on the GitHub Pages product repo.
